DAY 1: Skeleton, DB, Basic API (enqueue + status)
GOAL IS TO POST /jobs AND GET /jobs/:id AND THE ROW IS IN POSTGRES
    1. Create Go module + basic structure (and learn stuff) | DONE
    2. Postgres schema | DONE
    3. DB connection | DONE
    4. Job model + basic repo | DONE
    5. API server (basic) | DONE

DAY 2: Worker process + claim logic
GOAL IS TO START ONE OR MORE WORKERS AND THEY PULL PENDING JOBS AND MARK THEM DONE
    1. Worker binary entrypoint
    2. Implement "claim job" query
    3. Worker loop
    4. Mark job done 
    5. Test concurrency

DAY 3: Retries, backoff, DLQ, sweeper
GOAL IS TO RETRY JOBS THAT FAIL WITH BACKOFF AND MOVE TO DLQ. STUCK JOBS GET RECLAIMED VIA SWEEPER 
    1. Backoff + failed updates
    2. Simulate failing handler 
    3. Sweeper/reaper
    4. /stats API endpoint | DONE
    5. Chaos testing

DAY 4: Docker, polish, finish MVP
GOAL IS TO HAVE DOCKER-COMPOSE UP --BUILD GIVE A DB + API + MULTIPLE WORKERS